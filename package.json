{
  "name": "hackgu-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.11",
    "nodemailer": "^8.0.4"
  }
}
